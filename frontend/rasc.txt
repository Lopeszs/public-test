vou te mandar a estrutura da minha aplicação, eu gostaria que quando o botão "Enviar" fosse pressionado
a caixinha do recaptcha fosse ativada:

src/app/form/form.component.css:
.col-sm{
  margin: 2%;
}

.body{
  margin: 100px;
}

.br-checkbox{
  margin-top: 10px;
}

/* Garante que a lista apareça sobreposta aos outros elementos */
.br-list {
  /* DUAS COISAS IMPORTANTES PRO SELECT/CHECKBOX APARECER CORRETAMENTE. */
  /* display: block !important;
  position: unset; */

  
  z-index: 9999;
  width: 100%;
  background: white;
  box-shadow: 0 4px 6px rgba(0,0,0,0.1);
  border: 1px solid #ccc;
  margin-top: 5px;
}

/* Estilo básico para os itens da lista */
.br-item {
  padding: 10px;
  cursor: pointer;
}

.br-item:hover {
  background-color: #f0f0f0;
}

/* Garante que o container do select ocupe 100% da largura da coluna*/ 
.br-select {
  width: 100%;
}

/* Garante que o input dentro do select acompanhe o tamanho */
.br-select .br-input {
  width: 100%;
}

/* 1. Força o container do select a ocupar a largura total disponível */
.br-select {
  display: flex !important;
  flex-direction: column;
  width: 100% !important; /* Força o alinhamento total */
  min-width: 100% !important;
}

p{
  font-size: 90%;
}

.br-card{
  padding: 1%;

}

src/app/form/form.component.html:
<div class="ounder-md p-2x">
  <h1>Formulário para acesso aos dados</h1>

  <p>
    Lorem ipsum dolor sit amet, consectetur adipiscing elit. Nullam eleifend
    feugiat metus, vitae blandit nibh placerat sed. Proin finibus tincidunt
    venenatis. Pellentesque eget eros ac quam auctor eleifend nec sodales purus.
    Aliquam nec blandit justo, vel eleifend sapien. Vivamus sit amet enim leo.
    Nunc auctor lacus non arcu faucibus mollis. Pellentesque habitant morbi
    tristique senectus et netus et malesuada fames ac turpis egestas. Ut
    pellentesque id nunc et lacinia. Sed porttitor vehicula turpis, lobortis
    consectetur.
  </p>
</div>

<h3>Seleção de dados a serem baixados</h3>
<p>
  Lorem ipsum dolor sit amet, consectetur adipiscing elit. Nullam eleifend
  feugiat metus, vitae blandit nibh placerat sed. Proin finibus tincidunt
  venenatis. Pellentesque eget eros ac quam auctor eleifend nec sodales purus.
</p>

<!-- PRIMEIRO CARD: ESCOLHER DADOS A SEREM BAIXADOS -->
<form class="h-100" method="get">
  <div class="br-card rounder-md">
    <div class="card-header">
      <div class="br-input">
        <label for="meu-select">Dados a serem baixados</label>
        <div class="br-select" [class.is-active]="isDropdownOpen">
          <div class="input-group">
            <input
              id="meu-select"
              type="text"
              [value]="placeholderData()"
              readonly
              (click)="toggleDropdown($event)"
              placeholder="Selecione um ou mais conjunto de dados"
              required
            />
            <button
              class="br-button"
              type="button"
              (click)="toggleDropdown($event)"
            >
              <i class="fas fa-chevron-down"></i>
            </button>
          </div>
        </div>

        <div class="br-list" tabindex="0" *ngIf="isDropdownOpen">
          <div class="br-item" *ngFor="let d of data">
            <div class="br-checkbox">
              <input
                [id]="d.value"
                type="checkbox"
                name="select"
                [value]="d.value"
                (click)="selectData(d)"
                [checked]="isItemSelected(d)"
              />
              <label [for]="d.value">{{ d.label }}</label>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
  <!-- SEGUNDO CARD: FILTROS -->

  <h3 *ngIf="selectedData.length != 0">Filtragem de dados</h3>

  <div *ngIf="isItemSelected(data[0])">
    <app-filter-temas></app-filter-temas>
  </div>

  <div *ngIf="isItemSelected(data[1])">
    <app-filter-macrotemas></app-filter-macrotemas>
  </div>

  <!-- TERCEIRO CARD: DADOS DO USUARIO -->

  <h3>Dados para envio</h3>
  <div class="br-card rounder-md">
    <div class="card-header">
      <div>
        <!-- <div class="br-input">
          <label for="meu-select">Dados a serem baixados</label>
          <div class="br-select" [class.is-active]="isDropdownOpen">
            <div class="input-group">
              <input
                id="meu-select"
                type="text"
                [value]="selectedLabel"
                readonly
                (click)="toggleDropdown($event)"
                placeholder="Selecione o tipo"
                required
                pattern="[A-Za-z]{3}"
              />
              <button
                class="br-button"
                type="button"
                (click)="toggleDropdown($event)"
              >
                <i class="fas fa-chevron-down"></i>
              </button>
            </div>
          </div>

          <div
            class="br-list"
            *ngIf="isDropdownOpen"
            (mouseleave)="toggleDropdown($event)"
          >
            <div
              class="br-item"
              *ngFor="let item of opcoes"
              (click)="selectOption(item)"
            >
              {{ item.label }}
            </div>
          </div>
        </div> -->
        <div class="row gx-3 gy-3 align-items-end">
          <div class="col-12 col-lg-5">
            <div class="br-input">
              <label for="input-default">Nome do pesquisador</label>
              <input
                id="input-default"
                type="text"
                placeholder="Nome do pesquisador"
                name="nome-pesquisador"
                required
              />
            </div>
          </div>
          <div class="col-12 col-lg-5">
            <div class="br-input">
              <label for="input-default">E-mail para envio dos dados</label>
              <input
                id="input-default"
                type="text"
                placeholder="E-mail"
                required
              />
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- CHECKBOX DE TERMOS -->
  <div class="br-checkbox">
    <input id="checkbox-1" name="termo-aceito" type="checkbox" required />
    <label for="checkbox-1"
      >Eu aceito os
      <a href="">
        termos de utilização dos dados mantidos pelo Observatório da Evolução do
        Sistema Nacional de Pós-Graduação.</a
      ></label
    >
  </div>

  <!-- BOTÒES -->
<div class="mt-auto d-flex justify-content-end gap-2">
  <a class="br-button secondary mr-3" href=""> Cancelar </a>
  <button class="br-button primary mr-3" type="button" (click)="enviaEmail()">
    Enviar
  </button>
</div>

<!-- 🔥 RECAPTCHA (SÓ APARECE APÓS CLICAR) -->
<div *ngIf="mostrarCaptcha" style="margin-top: 20px;">
  <re-captcha
    siteKey="6LcO4J4sAAAAAM7peH5OBke6PwqRNtJ5P8qJ5HBD"
    (resolved)="onCaptchaResolved($event)">
  </re-captcha>
</div>
</form>


src/app/form/form.component.spec.ts:
import { ComponentFixture, TestBed } from '@angular/core/testing';

import { FormComponent } from './form.component';

describe('FormComponent', () => {
  let component: FormComponent;
  let fixture: ComponentFixture<FormComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [FormComponent]
    })
    .compileComponents();
    
    fixture = TestBed.createComponent(FormComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});


src/app/form/form.component.ts:
import { Component, inject } from '@angular/core';
import { CommonModule } from '@angular/common';
import { HttpClient } from '@angular/common/http';
import { FilterMacrotemasComponent } from '../filter-macrotemas/filter-macrotemas.component';
import { FilterTemasComponent } from "../filter-temas/filter-temas.component";
import { FormsModule } from '@angular/forms';
import { RecaptchaModule } from 'ng-recaptcha';
import { ReCaptchaV3Service } from 'ng-recaptcha';

@Component({
  selector: 'app-form',
  standalone: true,
  imports: [
    CommonModule,
    FilterMacrotemasComponent,
    FilterTemasComponent,
    FormsModule,
    RecaptchaModule
  ],
  templateUrl: './form.component.html',
  styleUrl: './form.component.css',
})
export class FormComponent {

  data = [
    { value: 'temas-estrategicos', label: 'Temas Estratégicos' },
    { value: 'macrotemas', label: 'Macrotemas' },
  ];

  isDropdownOpen = false;
  selectedData: any[] = [];

  // 🔥 RECAPTCHA
  mostrarCaptcha = false;
  captchaToken: string | null = null;

  private readonly API = 'http://127.0.0.1:8000/api';
  private http = inject(HttpClient);

  
  // 🔥 BOTÃO ENVIAR
  enviaEmail() {

    // 1️⃣ Primeiro clique → mostra captcha
    if (!this.mostrarCaptcha) {
      this.mostrarCaptcha = true;
      return;
    }

    // 2️⃣ Se já mostrou, valida captcha
    if (!this.captchaToken) {
      alert('Confirme o captcha!');
      return;
    }

    // 3️⃣ ENVIO REAL
    this.http.post('http://localhost:8000/api/validate-recaptcha', {
      token: this.captchaToken
    }).subscribe({
      next: (data: any) => console.log('Resposta do backend:', data),
      error: (err: any) => console.error('Erro:', err),
    });
  }

  onCaptchaResolved(token: string | null) {
    console.log('TOKEN GERADO:', token); // 👈 DEBUG

    if (!token) return;

    this.captchaToken = token;
    this.enviaEmail();
  }

  toggleDropdown(event: Event) {
    event.stopPropagation();
    this.isDropdownOpen = !this.isDropdownOpen;
  }

  selectData(item: any) {
    const ind = this.selectedData.findIndex((x) => x.value === item.value);

    if (ind == -1) {
      this.selectedData = [...this.selectedData, item];
    } else {
      this.selectedData = this.selectedData.filter(x => x.value !== item.value);
    }
  }

  isItemSelected(item: any): boolean {
    return this.selectedData.some((x) => x.value === item.value);
  }

  placeholderData(): string {
    if (this.selectedData.length == 0) {
      return "Selecione os conjuntos de dados desejados";
    }

    return this.selectedData.map(item => item.label).join(', ');
  }
}

src/app/form-button/form-button.component.css:
"vazio"

src/app/form-button/form-button.component.html:
<div class="p-3">
  <button class="br-button block primary mb-3" type="button" routerLink="form">Formulario de acesso aos dados
  </button>
</div>

src/app/form-button/form-button.component.spec.ts:
import { ComponentFixture, TestBed } from '@angular/core/testing';

import { FormButtonComponent } from './form-button.component';

describe('FormButtonComponent', () => {
  let component: FormButtonComponent;
  let fixture: ComponentFixture<FormButtonComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [FormButtonComponent]
    })
    .compileComponents();
    
    fixture = TestBed.createComponent(FormButtonComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

src/app/form-button/form-button.component.ts:
import { Component } from '@angular/core';
import { RouterLink } from "@angular/router";

@Component({
  selector: 'app-form-button',
  standalone: true,
  imports: [RouterLink],
  templateUrl: './form-button.component.html',
  styleUrl: './form-button.component.css'
})
export class FormButtonComponent {

}


src/app/app.config.ts:
import { ApplicationConfig } from '@angular/core';
import { provideRouter } from '@angular/router';
import { provideHttpClient } from '@angular/common/http';
import { provideRecaptchaV3 } from 'ng-recaptcha';

import { routes } from './app.routes';

export const appConfig: ApplicationConfig = {
  providers: [
    provideRouter(routes),
    provideHttpClient(),
    provideRecaptchaV3('SUA_SITE_KEY_V3')
  ]
};
